# 写真コンテストものさし 設計計画書

- 文書日: 2026-07-22（v2: 同日、三者ブレスト反映 / v3: 2026-07-30、受賞作リンクのみ・応募管理削除・実装状況追記）
- 状態: 実装先行中（本書は仕様と品質基準を示す文書として維持）
- 仮称: **写真コンテストものさし**（短い呼称は「写真ものさし」）
- 想定配置: `app-dev/photo-contest-monosashi/`
- 姉妹サイト: 「身体芸術・公募ものさし」「助成ものさし」
- 議論記録: `obsidian-vault/ideas/2026-07-22_写真コンテストものさし_三者ブレスト.md`

> この文書では、確認できた事実、そこからの設計判断、今後本人が決める事項を分けて記載する。
> サイト名、公開URL、ブランド色は未決定。

---

## 0. 実装状況と本書の位置づけ（2026-07-30 追記）

本書v1はもともと実装前の設計案だったが、2026-07-22〜26のCodexセッションで**実装が先行**した。本書は以後「あるべき仕様と品質基準」を示す文書として維持し、実装との差分を以下に記録する。

確認できた実装の現状（2026-07-30、`git log`・READMEによる）:

- Next.js（Vinext / Cloudflare Workers構成）の単一ページアプリ `app/page.tsx`。ローカル検証済み・未公開。
- 収録は106応募ルート（国内74・海外/国際32）＋47都道府県の地方公募調査台帳＋キュレーション/探索先11件。初期想定の12件を大きく超えている。
- データは `data/*.json`（opportunity型・条項ごとの8根拠セル）で、本書§8のcontest→edition→rulesスキーマとは異なる形で先行。
- `data/trends.json` に受賞傾向メモ・観察タグが既に存在する。**本日の決定（受賞作は公式アーカイブへのリンクのみ）により縮退対象**。
- 作業ツリーに未コミットの変更が残っている（他エージェントの作業の可能性があるため、本書の改訂では実装ファイルに触れていない）。

本書との主な差分（要整合。実装変更は本人確認のうえ行う）:

1. **スタック**: 本書は「サーバーなし静的サイト」を核とするが、実装はNext.js＋Workers構成。写真非送信・外部リクエストなしのプライバシー保証（§12.2）をこの構成でどう技術的に担保するか確認が必要。
2. **スキーマ**: edition分離・条項単位rulesは実装未反映。翌年更新時の上書きリスク（§8.2の更新原則）は現実装にもそのまま当てはまる。
3. **trends.json**: 観察・傾向メモを「公式アーカイブへのリンク」へ縮退する（本人決定 2026-07-30）。
4. **収録規模**: 「12件で検証してから拡大」の想定は実装が追い越した。以後は件数拡大よりも§14の品質検証と鮮度運用（§9）を優先する。

---

## 1. 結論

このサイトの中心は、コンテストの一覧ではなく、**「頭の中にある一枚を主語に、出せる場所と出す際の注意を測る」体験**にする。写真ファイルを置くことは**任意**とする（本人決定 2026-07-22）。画像は解析しないため、必須アップロードは不安と儀式を増やすだけだからである。

サイトが判定するのは**応募条件への適合だけ**である。三値で判定し、適合の主表示は「応募できます」ではなく**「確認した明示条件上、不一致なし」**とする。

過去の受賞作は本サイトへ収録せず、**主催者の公式アーカイブページへのリンク**を示すに留める（本人決定 2026-07-30）。作風が合うかは本人が公式ページを自分の目で見て判断する。観察タグ・「公開受賞作内の分布」表示はPhase 2以降の検討事項とし、導入する場合も受賞確率・AI採点を出さない原則は変えない。

このMVPの固有価値は次に集約される。

> 一枚の写真を起点に、年度・部門ごとの公式条項を根拠付きで照合し、複数候補間の規定の突き合わせまで本人が確認できる。写真は端末の外へ出ない。

---

## 2. 誰が、どの場面で、何を達成するか

### 主な利用者

- 日本に住み、国内外のフォトコンテストを探している個人の写真家
- プロ・アマチュアを問わない
- 「良い写真は撮れたが、どの公募に出すべきか分からない」人
- 応募料、撮影時期、既発表・他公募への応募、加工可否などを毎回読み解く負担を減らしたい人

### 主な利用場面

1. 撮影後、傑作だと思う一枚を頭に置いて質問に答え始める（写真を置くのは任意）。
2. 撮影時期、発表歴、加工などをコア5問で確認する。
3. 応募条件に当てはまりそうなコンテストだけを見る。
4. 候補の公式要項と受賞作アーカイブを開き、作品をどこへ出すか自分で判断する。
5. 同じ作品を複数へ出す場合、保存候補間の条項比較表で規定の抵触可能性に自分で気づく。

### 使い終えた後の気分

- 「大量の公募から探した」ではなく、**「この一枚の行き先が見えた」**と感じる。
- AIに作品の価値を採点された気分ではなく、自分の判断材料が増えたと感じる。
- 応募条件の見落としや、同時応募の衝突に対する不安が減る。

---

## 3. このプロジェクト固有の核

### 3.1 一覧起点ではなく「一枚起点」— ただし写真ファイルは任意

主語は常に「本人の頭の中にある一枚」である。トップ画面の主役は検索窓やカード一覧ではなく、コンタクトシートの空きコマのような**「作品の席（空のフレーム）」**にする。写真を置いても置かなくても、その一枚のための調べものとして扱う。

- 写真を置いた場合も、端末内プレビューにだけ使い、送信しない。
- 空のフレームが一般的なアップロード欄に見えないよう、`FRAME 01 / OPTIONAL` のような撮影文化の記号を添え、「写真なしで条件から探す」を主ボタンにする。

### 3.2 作品の価値をAIが決めない

「傑作かどうか」は本人の判断として尊重する。サイトが行うのは、応募規定を整理し、**どこに合い、どこが不明かを説明すること**である。

### 3.3 審査傾向を扱わない

MVPは受賞傾向・分布・観察タグを一切表示せず、公式アーカイブへのリンクだけを置く。将来（Phase 2以降）分布表示を導入する場合も、人格推測をせず、対象年・部門・標本数を明記し、受賞作しか見えない構造上「勝ちやすさ」は言えないことを明記する（原則の詳細は§7）。

### 3.4 同じ一枚を出し回る際の「抵触可能性」に気づける

既受賞、商用掲載、同時応募、類似カット、SNS掲載の扱いは公募ごとに異なる。MVPでは衝突の**自動判定は行わず**（本人決定 2026-07-22）、次の2段で本人が気づける状態を作る。

1. 候補カード上の注意表示（例: 「結果発表まで類似作の応募を制限」）
2. 保存候補間の**条項比較表**（§4 ステップ5）

応募状況をサイトは知らないため、表現は「規定衝突」と断定せず**「両方へ応募した場合に抵触する可能性」**とする。自動衝突判定・応募計画タイムラインはPhase 2の検討事項とする。

---

## 4. MVPの中心体験

### ステップ1: 作品の席に着く

```text
        ┌─ FRAME 01 / OPTIONAL ──────┐
        │                            │
        │  （コンタクトシートの空きコマ）│
        │                            │
        └────────────────────────────┘

        [ 写真なしで条件から探す ]      ← 主ボタン
        [ 写真を置いて始める（任意） ]   ← 副ボタン
```

表示文案:

> この一枚を、どこへ出せるか。
>
> 写真は任意です。追加した写真を、このサイトから送信することはありません。

- 写真を置いた場合、ファイル名・容量・ピクセル寸法・形式は可能な範囲で自動取得する。
- 画像そのものは保存しない。ページを閉じたら消える設計を既定とする。

### ステップ2: コア5問に答える

一度に長いフォームを見せず、1問ずつ答える。**固定コア5問**:

1. **撮影時期** — 撮影日または撮影年
2. **発表・受賞歴**（複数選択） — 未発表 / SNSのみ / 展示 / 出版・広告 / 過去入選・受賞
3. **AI・加工**（操作別の複数選択） — 色・露出補正のみ / トリミング / 合成 / 被写体の追加・削除 / 生成AI・生成塗り足し / 判断できない（要確認）
   ※「加工の程度」の段階評価にしない。規定上はAI生成・合成・補正が別条項だからである。
4. **単写真か組写真か**
5. **応募者条件** — 居住国、年齢または年代、プロ / アマチュア

**「他公募へ応募中か」はコア質問に入れない。**時間とともに変わる事実であり、全候補共通で先に聞く価値が低い。同時応募・類似作規定が「禁止 / 条件付き / 要確認」の候補についてだけ、候補別の解決質問として聞く（§6.3）。

### ステップ3: 候補を見る

結果は3群に分ける。

- **確認した明示条件上、不一致なし** — 「応募条件に適合」「応募できます」という強い表示は使わない。
- **要確認** — 要項が曖昧、または作品カルテに未回答がある。
- **明示条件に不一致** — 撮影年、発表歴、加工など、明確に合わない。

「要確認」の候補カードには**候補別の解決質問**を出す。

- カードに「この公募についてあと◯問」と表示し、タップするとその公募の未確認項目だけを聞く。
- 文言は「あと1問で確定します」ではなく**「この回答で未確認項目が1つ減ります」**とする。要項自体の曖昧さや鮮度の問題は回答では消えないためである。
- グローバルな動的質問選択（情報利得計算）はMVPでは行わず、Phase 2で実データの分岐実績を見てから判断する。

候補カードでは単一の「マッチ度」を出さず、次を分けて表示する。

```text
明示条件        8項目不一致なし / 未確認1項目
締切            2026-09-30 17:00 JST（7/22公式確認）
応募コスト      無料 / 入賞後の展示費は要確認
受賞作          公式アーカイブを見る ↗（外部サイト）
注意            同時応募不可。結果発表まで他公募へ出せない
```

### ステップ4: 公式アーカイブへの導線

コンテスト詳細では、受賞作を本サイトに収録せず、次だけを置く。

- 確認できた年度別の公式アーカイブ・受賞者発表ページへの外部リンク
- 公式が示した審査基準（リンクで案内。転載する場合は§7.2のテキストポリシーに従う）
- リンク先が外部サイトであることの明示

受賞作の画像・キャプション・観察タグ・集計をサイト内に持たない。リンク切れの検査を運用に含める（§9.3）。

### ステップ5: あとで見る・条項比較表で抵触可能性に気づく

- 保存するのは**候補ルートの識別子（コンテスト＋開催回＋部門の組）**と、永続化を明示選択したカルテ回答だけ。画像は保存しない。
- 応募状況（検討中 / 応募済み / 結果待ちなど）の管理は**持たない**（本人決定 2026-07-30）。
- 開催回・部門まで保存する理由: 規定はedition / category単位のため、コンテストIDだけでは以前見た判定・比較を再現できない。
- 保存候補間の**条項比較表**を表示する。
  - 行 = 規定条項（同時応募 / 類似作 / 既受賞 / 発表歴 / AI / 加工 / 結果発表までの拘束）
  - 列 = 保存候補
  - セル = 規定値＋根拠リンク＋未確認マーク
- セルの規定値は5値で区別する: **許可 / 禁止 / 条件付き / 公式要項に記載なし / 未調査**。「記載なし」を「許可」と表示せず、「記載なし」と「未調査」も統合しない。
- 各条項に**対象範囲**（同一作品 / 類似作品 / 同一シリーズ / 作家の全作品）と**拘束期間**（開始・終了の条件。例: 応募時から結果発表まで）を併記する。
- 比較表は、解決質問への回答の有無に関係なく規定そのものを常時表示する。
- 表の上には自動判定の「衝突あり」ではなく、根拠付きの注意を出す。

> 例: Aは結果発表まで類似作の応募を制限しています。Bと両方へ出す場合は抵触する可能性があります。

- MVPでは「最適な応募順」を自動決定しない。

### 作品カルテの質問プール（解決質問の出所）

コア5問以外の質問は、候補別の解決質問として必要になったときだけ聞く。プールは次のとおり。

#### A. 作品の形

- 縦 / 横 / 正方形 / パノラマ、カラー / モノクロ、デジタル / フィルムをスキャン
- ファイル形式、容量、ピクセル寸法、色空間（写真を置いた場合は自動取得値を確認）

#### B. 撮影と発表の履歴（詳細）

- 他の公募へ応募中か・結果待ちか（同時応募規定のある候補の解決質問。要項に応じて「同一作品か類似作か」「取り下げ可能か」まで分岐する。**回答はそのセッション内だけで使い、応募状況として保存しない**）
- 同じ撮影機会の類似カットを別公募へ出しているか
- 商用利用の有無・範囲

#### C. 編集・生成（詳細）

- 複数枚の合成、カメラ内AI補正など、本人が判断できない項目は「要確認」を選べる

#### D. 被写体と権利

- 人物の顔が識別できるか、未成年が写っているか
- 被写体の同意・モデルリリースがあるか
- 私有地、作品、商標、文化財など第三者の権利物が中心か
- 野生動物、ドローン、撮影禁止場所など特別な規定に関係するか
- 作品の著作権を本人が持つか

#### E. 内容と表現

- 被写体: 人物、街、自然、野生動物、建築、静物、スポーツ、舞台、抽象など
- 意図・見え方は部門選びの補助としてだけ使う（傾向照合には使わない）

#### F. 応募者本人（詳細）

- 国籍・学生か（必要な公募だけ）
- 使用カメラのメーカー・機種（メーカー限定賞などに必要な場合だけ）
- 応募に使える言語
- 応募料・送料・展示費の許容額

---

## 5. 情報設計

### 推奨ページ構成

| ページ | 役割 |
| --- | --- |
| `/` | 作品の席(空のフレーム)とコア5問から始める主画面 |
| `/contests.html` | 質問に答えずに全コンテストを見る一覧 |
| `/contests/{id}.html` | 応募条件、部門、費用、権利、公式アーカイブリンク、出典を確認する詳細 |
| `/saved.html` | 保存候補の条項比較表 |
| `/methodology.html` | 判定の方法、限界、出典の扱いを説明する |
| `/about.html` | サイトの目的、姉妹サイトとの関係 |
| `/privacy.html` | 写真・作品カルテ・保存データの扱い |
| `/disclaimer.html` | 免責、訂正窓口 |

`/calendar.html` はMVPから削除（Phase 2で必要性を再検討）。

### ナビゲーション

- この一枚に合う公募
- コンテスト一覧
- あとで見る
- 見方・調べ方

「ダッシュボード」風の機能名ではなく、写真家が次にしたい行動を言葉にする。

---

## 6. 判定ロジック

### 6.1 三値判定を基本にする

各ルールは次のいずれかを返す。

- `pass`: 明示条件に不一致なし
- `needs_check`: 条件または回答が不明
- `fail`: 明示条件に不一致

一つでも `fail` があれば「明示条件に不一致」。`fail` がなく `needs_check` があれば「要確認」。すべて `pass` の場合だけ「確認した明示条件上、不一致なし」とする。

「不一致なし」は主催者による資格認定ではない。**掲載要項の明示条件で不一致を確認できなかった**という意味に限定し、見出し・カード・詳細のすべてでこの表示名を使う（説明文だけで限定しない。見出しの方が記憶されるため）。

### 6.2 判定対象

- 応募者: 年齢、居住地、国籍、学生、プロ・アマ、会員除外
- 作品: 撮影期間、テーマ、単写真・組写真、ファイル、プリント、カメラ限定
- 発表歴: SNS、展示、出版、商用、過去受賞
- 重複: 同時応募、類似作品、同一シリーズ（**判定対象から外さない**。応募資格を直接左右するため、比較表の情報表示だけに隔離しない）
- 加工: 色調補正、トリミング、合成、要素削除、生成AI
- 権利: 著作権、肖像、モデルリリース、第三者著作物
- 費用: 応募料、送料、入賞後のプリント・展示・パネル代
- 応募実務: 応募言語、締切時刻、タイムゾーン、送付先、オンライン可否

### 6.3 質問の出し方（2段構成）

1. **固定コア5問**（§4 ステップ2）で全候補を仮判定する。
2. **候補別の解決質問**で「要確認」を1つずつ減らす。質問はその公募の未確認項目だけに限定する。

同時応募・類似作の解決質問は、該当規定を持つ候補にだけ表示する。未回答なら `needs_check`、禁止規定に該当すれば `fail`。回答は照合セッション内でのみ使い、永続保存しない。

### 6.4 並び順

既定の並び順は次の優先度とする。

1. 明示条件に不一致がない
2. 受付中で、公式確認日が新しい
3. 締切が近い
4. 未確認項目が少ない
5. 本人が選んだ被写体・形式の部門がある

### 6.5 禁止する表示

- 受賞確率
- AIによる写真の点数
- 「この作品なら勝てる」
- 標本数を隠した傾向断定
- 公式の審査基準と、本サイト独自の観察を混ぜた説明
- 「応募できます」「応募条件に適合」という断定形の見出し
- 要項に記載がない事項を「許可」と表示すること（記載なし≠許可）
- 保存候補間の抵触を「衝突している」と断定すること（「両方へ応募した場合に抵触する可能性」に留める）

---

## 7. 受賞作アーカイブの扱い

### 7.1 MVP: 公式リンクのみ（本人決定 2026-07-30）

- 受賞作・観察タグ・分布集計・標本数・観察者情報を、データにもUIにも持たない。
- コンテスト詳細と候補カードに「公式受賞作を見る」外部リンクを置くだけとする。
- 受賞作ページは、現在年度の応募条件を証明する根拠には使わない（要項が根拠、アーカイブは作風確認用）。
- 削除し忘れの確認箇所: スキーマ、ナビゲーション、説明文、FAQ・SEO文言、テスト・fixture、品質監査、MVP受け入れ条件（`data/trends.json` の観察・傾向メモは縮退対象）。

### 7.2 テキスト転載の原則

公式キャプション・審査評などのテキストの転載・翻訳は著作権の対象になる。掲載する場合は次を区別する。

- `link_only`: リンクのみ（既定値）
- `paraphrased`: 本サイトの言葉で要約（元の表現を再現しない）
- `quoted_with_basis`: 引用の要件（必要性、区別、主従関係、出典）を検討したうえでの短い引用
- `licensed`: 掲載許諾を確認済み

「出典を付ければ引用は常に適法」とは扱わない。

### 7.3 Phase 2で分布表示を導入する場合の原則（保留）

導入する場合は、v2で定めた次の原則に従う。

- **3層分離**: 層A=確認できる事実（年・部門・賞・作品名・公式URL）/ 層B=編集部の観察タグ（`taggedBy`・`reviewedAt`・`confidence` つき）/ 層C=集計からの解釈（対象年・部門・点数を必ず添える）。
- **生存バイアスの定型注記**: 「受賞作のみの観察であり、応募作全体や受賞のしやすさを示すものではない」。
- **標本数の運用基準**: 1〜4点は個別表示のみ、5〜11点は「参考」、12点以上で比率表示。
- **母集団を混ぜない**: 総合賞と部門賞、単写真と組写真、テーマ・審査員が変わった年度を無条件に合算しない。
- **画像の権利**: `link_only` を既定とし、許諾・ライセンス確認済みだけサイト内表示（`remote_embed_allowed` / `licensed_local_copy`）。

---

## 8. データ設計

### 8.1 単一の情報源

```text
data/
  contests.data.json          コンテスト主体（年度をまたいで不変の情報のみ）
  editions.data.json          開催回（年度・状態・日程・公式アーカイブURL・確認記録）
  categories.data.json        部門（開催回に属する）
  rules.data.json             条項単位の規定
  rule-evidence.map.json      条項と根拠の多対多対応
  source-evidence.data.json   出典（URL、PDFページ、該当条文の位置）
rules/
  match-rules.mjs             三値判定ルール
```

受賞作関連（winning-works / observations）はPhase 2で分布表示を導入する場合のみ追加する。

> 実装注: 現実装は `data/domestic-opportunities.json` 等のopportunity型スキーマ（条項ごとの8根拠セル）で先行している。上記への移行は差分の大きさを見て本人と決める。少なくとも「開催回の分離」と「前年凍結の更新原則」は、どちらのスキーマでも守る。

### 8.2 コンテスト主体と開催回（edition）

締切・状態・規約はコンテスト直下に持たせない。実体は `コンテスト → 開催回 → 部門 → 条項 → 根拠` である。開催回を分けないと、翌年の更新で過去条件を上書きしてしまう。

```json
{
  "id": "example_contest",
  "name": "コンテスト名",
  "organizer": "主催者",
  "officialSiteUrl": "https://example.com/"
}
```

```json
{
  "id": "example_contest_2026",
  "contestId": "example_contest",
  "round": "第51回",
  "year": 2026,
  "status": "open | expected | closed | rolling | unknown | retired",
  "officialArchiveUrl": "https://example.com/winners/",
  "events": [
    { "type": "entry_open", "at": "2026-07-01T00:00:00+09:00", "source": "ev_001" },
    { "type": "entry_deadline", "at": "2026-09-30T17:00:00+09:00", "source": "ev_001" },
    { "type": "arrival_deadline", "at": null, "source": null },
    { "type": "result_announcement", "at": null, "source": null },
    { "type": "exhibition_start", "at": null, "source": null }
  ],
  "verifiedAt": "2026-07-22",
  "nextCheck": "2026-08-15"
}
```

- 締切は単一フィールドにせず、応募開始・通常締切・作品到着期限・結果発表・展示期間を**別イベント**として保存する。タイムゾーンが書かれていなければ補完しない。
- `officialArchiveUrl` は受賞作アーカイブへの外部リンク用。確認できない場合は `null`（リンクを出さないだけで、収録可否には影響しない）。
- **更新原則: 前年editionは変更しない。**新editionを作り、規定を未確認状態（`needs_reverification`）でコピーし、前年との差分を確認してから公開する。前年確認済みだからといって新年度を確認済みにしない。

### 8.3 部門

```json
{
  "id": "example_contest_2026_single_portrait",
  "editionId": "example_contest_2026",
  "categoryFamilyKey": "single_portrait",
  "name": "単写真・人物",
  "theme": "自由",
  "workType": "single",
  "subjects": ["portrait"],
  "captureStart": "2026-01-01",
  "captureEnd": "2026-12-31",
  "maxEntries": 3,
  "file": { "mime": ["image/jpeg"], "minMB": 1, "maxMB": 5, "colorSpace": ["sRGB"] },
  "officialCriteria": ["構図", "創造性"],
  "criteriaSource": "ev_002"
}
```

- 部門IDは年度間で使い回さない。年度をまたぐ対応関係は `categoryFamilyKey` だけで持つ（部門名の変更・統廃合に耐えるため）。

### 8.4 条項（rules）

`social_allowed` のような単純な列挙値では「結果発表まで不可」「主催者の許可があれば可」「同一シリーズのみ不可」を表現できない。規定は条項単位で構造化する。

```json
{
  "id": "example_contest_2026_rule_012",
  "editionId": "example_contest_2026",
  "scope": { "type": "category", "categoryId": "example_contest_2026_single_portrait" },
  "priority": 10,
  "clause": "similar_work",
  "value": "conditional",
  "subjectScope": "same_series",
  "lockPeriod": { "from": "entry", "until": "result_announcement" },
  "conditions": "同一シリーズの類似カットは結果発表まで他公募へ応募不可",
  "exceptions": "主催者の書面許可がある場合を除く",
  "verification": "verified | needs_reverification"
}
```

- `scope`: 開催回全体の規定か、特定部門だけの規定かを区別し、`priority` で例外規定の優先順位を表す。
- `value` は5値: `allowed / prohibited / conditional / not_stated / not_checked`。**`not_stated`（記載なし）と `allowed`（許可）を混同せず、`not_stated` と `not_checked`（未調査）も統合しない。**
- `subjectScope`: 同一作品 / 類似作品 / 同一シリーズ / 作家の全作品。
- `lockPeriod`: 拘束の開始・終了条件。
- 根拠は `rule-evidence.map.json` の多対多対応で持つ。1条項に複数根拠、1つのPDF箇所に複数条項があり得るため、単数 `evidenceId` にしない。

### 8.5 作品カルテと端末内保存

- 画像をデータ化してJSONへ埋め込まない。GPS座標は取得・保存しない。
- カルテの回答は**既定でメモリ保持のみ**（ページを閉じたら消える）。回答には年齢・国籍・未成年被写体・同意状況など機微な内容が含まれ、共有端末で永続ストレージに残すべきではないためである。
- 永続保存は明示選択とし、その際に保存される内容を表示して同意を取る。
- 保存候補は**「コンテスト＋開催回＋部門」の識別子の組**をlocalStorageに保存する（応募状況のステータスは持たない）。
- 同時応募の解決質問への回答は保存しない（セッション内のみ）。
- 既存実装のlocalStorageに旧形式データがある場合は、保存候補IDを保ったまま旧フィールドを無視する移行処理を入れる。
- 「カルテを消す」「保存データをすべて消す」操作を常に表示する。

---

## 9. 情報の鮮度と運用

### 9.1 応募条件は一次情報だけ

- 現年度の主催者公式ページまたは公式PDFを優先する。
- SNS告知は主催者公式アカウントで、要項本文が他にない場合の補助資料とする。
- まとめサイトや検索結果だけでは「受付中」にしない。
- 旧年度しか確認できない場合は `expected` または `unknown` とし、「例年受付中」と断定しない。

### 9.2 fail-closed（強制力まで実装する）

レポートを出すだけでは鮮度は維持されない。次を仕組みとして実装する。

- 受付中案件の確認日が運用基準を超えたら、**ビルド時に自動で「要再確認」へ降格**して公開する。人が公式を確認して `verifiedAt` を更新した場合だけ「受付中」へ戻る。
- 締切7日前・24時間前の未再確認、`nextCheck` 超過は**ビルド時レポートに出力し、基準超過はビルド失敗**にする。
- 締切、受付状態、応募料、入賞後費用、権利、AI・加工規定はAIだけで自動変更しない。
- タイムゾーンや締切時刻が書かれていなければ補完しない。

### 9.3 公式アーカイブリンクの更新

- 結果発表後、新年度のアーカイブリンクを追加する。
- リンク切れ・リンク先の変更をビルド時または定期の検査対象にする（外部URLの死活確認は手動でもよいが、確認日を記録する）。

### 9.4 訂正窓口

- 応募条件の誤り
- 受付終了・日付変更
- 公式リンク・アーカイブリンクの誤り、リンク切れ

---

## 10. ビジュアル・UI方針

### 10.1 推奨方向: 「暗室のコンタクトシート × 手描きのものさし」

写真サイトにありがちな全面黒背景のギャラリーや、汎用的なカード型ダッシュボードにはしない。質問に答える前は和紙に近い明るい余白、写真を置いた場合は周囲だけが少し暗くなり、ライトテーブルで一枚を測っている感覚を作る。

- 背景: 温かいオフホワイト
- 文字: 墨に近いチャコール
- アクセント: 暗室灯を思わせる朱赤または深い琥珀
- 罫線: コンタクトシート、トリミング線、ものさしの目盛り
- 写真枠: 角丸カードではなく、余白と細いフレームで区切る
- 結果一覧: 同形カードの反復ではなく、写真台の横に差し込む応募票・検討票のような行

### 10.2 シリーズ共通性

- クリーム色の木・紙のような「ものさし」キャラクター
- 揺れる墨線と薄い水彩
- 数字や文字のない目盛り
- 企業マスコット風に磨きすぎない手描き感

写真版のキャラクター案は、ものさしが写真を採点する姿ではなく、**写真の端にそっと当てて、行き先を測る姿**にする。ルーペ、コンタクトシート、紙焼き用トングのいずれかを小道具候補とする。実制作前にユーザー確認が必要。

### 10.3 ファーストビュー

```text
写真コンテストものさし                         [コンテスト一覧] [あとで見る]

             この一枚を、どこへ出せるか。

        ┌─ FRAME 01 / OPTIONAL ────┐
        │                          │
        │ （コンタクトシートの空きコマ）│
        │                          │
        └──────────────────────────┘

        [ 写真なしで条件から探す ]
        [ 写真を置いて始める（任意） ]

        写真は任意です。追加した写真を、
        このサイトから送信することはありません。
```

### 10.4 モバイル

- 390px幅を基準にする。
- 写真を置いた場合は、小さなプレビューを上部に残し、質問を1問ずつ表示する。
- タップ領域は44px以上。
- 選択肢は横に詰めず、長い翻訳ラベルでも折り返せる。
- 結果を開いても対象の一枚を見失わないよう、写真サムネイル（または空のフレーム記号）を小さく固定する。
- 条項比較表は横スクロールを許容しつつ、条項名の列を固定する。

### 10.5 アクセシビリティ

- 色だけで pass / needs_check / fail を区別しない。
- キーボードだけで写真選択以外の全操作が可能。
- `prefers-reduced-motion` に対応。
- 写真上の文字重ねを避ける。

---

## 11. 収録範囲と品質基準

### 現状と方針

初期想定（12開催回で検証してから拡大）は実装が追い越し、既に106応募ルート＋47都道府県台帳＋探索先11件が収録されている。以後は**件数の拡大よりも品質と鮮度を優先**する。

- 新規追加より、収録済みルートの§14検証・確認日更新・リンク検査を先に回す。
- 拡大する場合も、被写体・形式・国内外・無料有料の偏りを見て選ぶ。

### 選定の基準

- 日本から応募できる（必須）
- 現行の公式要項を確認できる（必須）
- 日本語または英語で応募できる（主要基準）
- 条件の多様性・対象写真家にとっての有用性（主要基準）
- 受賞作の公式アーカイブがある（**加点要素**。必須にすると歴史の浅い公募や地域公募が不当に除外されるため、必須基準にしない）

---

## 12. 技術方針

### 12.1 構成

本書v1〜v2は「依存を抑えた静的サイト（Node ESMの `build.mjs`、サーバーなし）」を推奨したが、実装はNext.js（Vinext / Cloudflare Workers）で先行している。構成を戻すか続行するかは本人判断とし、**どちらの場合も次を必須とする**。

- 判定はクライアント側で完結し、写真・カルテ回答をサーバーへ送らない
- 写真プレビューは File API / Object URL
- カルテ回答は既定メモリ保持、保存候補識別子は localStorage
- ログイン・画像アップロードなし

### 12.2 プライバシーの技術的裏付け

「静的サイトだから安全」「サーバーへ送らない」だけでは保証にならない。同一ページで動く第三者スクリプトはデータへ触れ得る。

- 外部JS・解析タグ・広告を置かない
- フォントも自己配信する
- CSP（Content-Security-Policy）で通信先を制限する
- 写真選択時・カルテ入力時に外部リクエストが発生しないことのネットワーク検査を完成条件とする
- 「端末内処理だから法的問題なし」とは書かない。人物が識別できる写真は個人情報に当たり得るため、取得・保存・第三者送信がないことを技術的に説明する
- サーバー構成（Workers）を続行する場合は、判定・カルテがサーバーへ渡らないことを検査項目に含める

### 12.3 画像AI・EXIFは後段

- 画像AIはPhase 2以降の選択機能。既定オフ、送信先・保存期間・目的・費用を送信前に表示、「タグの提案」に限定、本人承認後に反映。
- EXIF読み取りもPhase 2で、明示操作・本人確認後採用の方式とする（撮影日は欠落・編集・時計ずれがあり、結局本人確認が必要なため）。

### 12.4 公開先

シリーズの運用を揃えるならGitHub Pagesと独自サブドメインが第一候補。ただし現実装のWorkers構成では公開手段が異なるため、スタック判断と合わせて決める。公開URLとサブドメインは未決定。

---

## 13. 段階実装（実装先行後の読み替え）

実装が先行したため、本節は「未実装項目の抽出と検収基準」として読む。

### 現時点の主な未実装・要整合項目

1. 受賞作関連の縮退: `data/trends.json` の観察・傾向メモを公式アーカイブリンクへ置き換える（本人決定 2026-07-30）。
2. 応募状況管理を実装しない方針の確認（現実装に該当機能があれば削除）。
3. 保存候補の識別子を「コンテスト＋開催回＋部門」の組にする。旧localStorageの移行処理。
4. 開催回（edition）分離と「前年凍結・未確認コピー」の更新原則の反映。
5. 同時応募の候補別解決質問（セッション内のみ・保存なし）。
6. 鮮度の強制力（自動降格・ビルド失敗）。
7. CSP・外部リクエストなしのネットワーク検査。
8. 「確認した明示条件上、不一致なし」等の表示名統一（現実装は「確認範囲で不一致なし」— 意味は同等。統一するかは表記の好みで決めてよい）。

### Phase 1.5 — 実地テスト（未実施・優先）

ユーザー本人の写真3〜5枚で試す。人物、風景、抽象、単写真、シリーズなど性格の違う作品を使い、次を記録する。

- 候補が0件になった理由は妥当か
- コア5問＋解決質問の負担は適切か
- 「要確認」が多すぎないか
- 写真を置く導線と置かない導線のどちらを自然に選んだか
- 公式アーカイブを開いて応募判断が変わったか
- 条項比較表で抵触可能性に自分で気づけたか

### Phase 2 — 拡張（MVPから意図的に外したもの）

- 受賞作の収録・観察タグ・分布表示（§7.3の原則に従う。許諾済みサムネイル含む）
- 応募状況管理（検討中 / 応募済み / 結果待ち / 結果の記録）
- 自動衝突判定・応募計画タイムライン（拘束期間の曖昧さを確定情報に見せない表現を検討してから）
- カレンダーページ・締切の端末内カレンダー
- 複数作品のカルテ切り替え（画像は保存しない）
- EXIF読み取り（明示操作・本人確認後採用）
- 動的質問選択（分岐実績データを見てから)
- 必要性が確認できた場合だけ、任意のAIタグ提案

---

## 14. 検証計画

### データ

- ID重複0（開催回・部門IDの年度間使い回し0）
- 必須項目欠落0
- 不正な日付・タイムゾーン0
- 公式URL形式不正0
- 現行回と過去回の混同0（前年editionへの変更が入っていないこと）
- 新editionへコピーされた条項がすべて `needs_reverification` であること
- 応募料と入賞後費用の混同0
- 部門と開催回の条項継承（scope・priority）の解決が仕様どおり
- 公式アーカイブリンクの形式検査と確認日記録
- 受賞作・観察タグ・集計値がデータに存在しないこと（Phase 2導入まで）

### 判定

- 境界年齢、撮影日の開始・終了日
- SNS掲載可 / 商用掲載不可の区別
- 過去受賞不可 / 同時応募不可 / 類似作不可を別条項として検査
- `not_stated` が `allowed` として表示されないこと
- 未回答の同時応募条件が安全側で `needs_check` になること
- 単写真とシリーズの混同防止
- JPEGの容量上限・下限
- AI禁止、合成禁止、基本補正可の組み合わせ
- 未回答は `fail` ではなく `needs_check`

### UI

- 390px / 1024px
- 写真なし導線・写真あり導線の両方で完了できる
- 画像が縦長、横長、巨大、極小でも崩れない
- HEICなど未対応形式で代替案を説明する
- キーボード操作
- VoiceOverで判定理由を読める
- 44px以上のタップ領域
- `prefers-reduced-motion`
- 比較表の注意文言が「抵触の可能性」表現になっている（断定していない）

### プライバシー

- 写真選択時・カルテ入力時に外部リクエストが発生しない（CSPで技術的に制限）
- 外部JS・外部フォント・解析タグが存在しない
- カルテ回答が明示選択なしに永続保存されない
- localStorageに画像・ファイルパス・GPS座標・応募状況・同時応募回答を入れない
- 「カルテを消す」「すべて消す」で保存データを削除できる
- 外部公式ギャラリーを開く前に外部サイトであることが分かる

---

## 15. 成功指標

初期はアクセス数より、判断に役立ったかを測る。

- 質問を始めた人が候補結果まで到達した割合
- 1作品あたり「不一致なし」または「要確認」が1件以上見つかる割合
- 結果から公式要項を開いた割合
- 公式アーカイブリンクを開いた割合
- 条項比較表を見て応募判断を変えた例があるか
- 「明示条件の判定」を「受賞のしやすさ」と誤認した利用者がいないか
- ユーザー本人が3〜5枚で試し、「この候補は自力では見つけにくかった」と感じる例があるか

写真自体や作品カルテを解析サーバーへ送らないMVPでは、詳細な行動計測も既定でオフにする。テスト時は本人の観察記録を優先する。

---

## 16. 主なリスクと対策

| リスク | 対策 |
| --- | --- |
| 「適合」表示を主催者のお墨付きと誤解する | 主表示を「確認した明示条件上、不一致なし」に統一 |
| 古い要項で誤判定する | 現行一次情報、確認日、fail-closedの自動降格、ビルド失敗 |
| 翌年更新で過去条件を上書きする | edition分離、前年凍結、新editionは未確認コピーから |
| 「記載なし」を「許可」と誤読させる | 条項値5値（記載なし・未調査を明示・統合しない） |
| 比較表の注意を「違反確定」と誤解する | 「両方へ応募した場合に抵触する可能性」表現に統一 |
| 公式アーカイブのリンク切れ・移転 | ビルド時・定期のリンク検査、確認日を記録 |
| 写真アップロードへの不安 | 写真は任意。置いた場合も端末内処理・非保存、CSP＋通信検査 |
| 機微な回答が共有端末に残る | カルテ回答は既定メモリ保持、永続保存は明示選択 |
| 質問が多く離脱する | コア5問＋候補別解決質問。全カルテを先に埋めさせない |
| 同時応募規定の解釈を誤る | 自動判定せず、条項・対象範囲・拘束期間・根拠を並べて本人が判断 |
| 候補0件で行き止まりになる | 0件時に除外理由を説明 |

---

## 17. 決定事項と残る論点

### 決定済み

| 論点 | 決定 | 日付 |
| --- | --- | --- |
| 写真の扱い | 任意。置いた場合も端末内のみ、送信・保存なし | 2026-07-22 |
| 判定表示名 | 「確認した明示条件上、不一致なし」 | 2026-07-22 |
| 衝突検出 | 自動判定なし。カード注意表示＋条項比較表まで | 2026-07-22 |
| データ設計 | edition導入、条項単位rules、根拠は多対多 | 2026-07-22 |
| カルテ保存 | 既定メモリ保持、永続保存は明示選択 | 2026-07-22 |
| **受賞作アーカイブ** | **収録・観察タグ・分布表示なし。公式ページへのリンクのみ** | **2026-07-30** |
| **応募状況管理** | **持たない。保存は候補ルート識別子（コンテスト＋開催回＋部門）のみ** | **2026-07-30** |
| 同時応募の扱い | コア質問に入れず、候補別解決質問（セッション内のみ・保存なし） | 2026-07-30 |
| アーカイブ有無の位置づけ | 選定基準の必須ではなく加点要素 | 2026-07-30 |
| MVPから除外 | カレンダー、複数カルテ、EXIF、タイムライン、自動衝突判定、動的質問選択、受賞作収録、応募状況管理 | 2026-07-22 / 07-30 |

### 実装前後で本人が決めること（推奨初期値つき）

| 論点 | 推奨初期値 |
| --- | --- |
| サイト名 | 仮称「写真コンテストものさし」 |
| スタック | 現実装（Next.js/Workers）続行か、静的構成へ寄せるか。§12.2の担保が可能な方 |
| trends.json の縮退時期 | 次の実装作業時にリンクのみへ置き換え |
| 対象ユーザー | 個人、プロ・アマ両方 |
| ブランド色 | 暗室灯の朱赤 / 深い琥珀を候補に、実装前に比較 |
| キャラクター小道具 | ルーペ / コンタクトシート / 紙焼き用トングから選定 |
| 公開 | 本人テスト後に決定（Pages＋サブドメイン or Workers） |

---

## 18. 事実確認に使った一次情報

以下は設計時のデータ項目と運用リスクを確認するために参照した。各コンテストを掲載する際は、改めて現行回の要項を確認する。

- [Nikon Film and Photo Contest — Photo Entry Guideline](https://www.nikon-photocontest.com/en/photo/entryguideline/)
- [Nikon Film and Photo Contest — Winners Archive](https://www.nikon-photocontest.com/en/photo/winners/)
- [Sony World Photography Awards — Single Image Competition](https://www.worldphoto.org/sony-world-photography-awards/single-image)
- [Sony World Photography Awards — Winners Galleries](https://www.worldphoto.org/sony-world-photography-awards/winners-galleries)
- [日本写真家協会 — 第51回2026JPS展 応募案内](https://www.jps.gr.jp/2026jpsten_oubo/)
- [日本写真家協会 — 2026JPS展 受賞者発表](https://www.jps.gr.jp/2026jpsten_winner/)
- [Kenko Filters Photography Awards 2026 — 応募要項](https://www.kenko-tokina.co.jp/contest/kfpa/2026/application.html)

設計判断の参考にした一般資料（Codexレビューで提示）:

- [WHATWG HTML — Web Storage（永続データの機微性への注意）](https://html.spec.whatwg.org/multipage/webstorage.html)
- [W3C — Content Security Policy](https://www.w3.org/TR/CSP/)
- [文化庁 — 著作権に関するFAQ（引用の要件）](https://www.bunka.go.jp/seisaku/bunka_gyosei/kibankyoka/faq/index.html?s=09)
- [個人情報保護委員会 — 通則ガイドライン（顔画像の個人情報該当性）](https://www.ppc.go.jp/personalinfo/legal/guidelines_tsusoku/)

---

## 19. MVP完成条件

- 写真を置かずに、コア5問から候補確認まで完了できる（写真を置いた場合は端末内プレビューされる）。
- 収録ルートを `不一致なし / 要確認 / 明示条件に不一致` に分け、全理由を条項と出典つきで説明できる。
- 候補別の解決質問で「要確認」を減らせる。同時応募の回答は保存されない。
- 受賞作・画像・キャプション・観察タグをサイト内に持たず、公式アーカイブへのリンクだけがある。
- 保存候補間の条項比較表で、対象範囲・拘束期間・記載なしの区別を含めて確認でき、注意文言が「抵触の可能性」表現になっている。
- 保存候補が「コンテスト＋開催回＋部門」で識別され、翌年更新後も以前の判定を再現できる。
- 写真、ファイルパス、GPS座標を送信・永続保存しない。カルテ回答の永続保存は明示選択のみ。
- CSPが設定され、外部JS・外部フォント・解析タグがなく、ネットワーク検査を通る。
- 前年editionを凍結したまま新editionを追加できることをデータ検証で確認できる。
- 390px幅で中心体験が完了する。
